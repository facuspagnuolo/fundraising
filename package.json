{
  "name": "fundraising",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "fix": "rm -rf **/websocket/.git",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
